import users from "./fake.api/users.api";
import messages from "./fake.api/messages.api";
import posts from "./fake.api/posts.api";
import profile from "./fake.api/profile.api";

const api = {
    users,
    messages,
    posts,
    profile
};
export default api;
